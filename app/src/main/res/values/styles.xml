<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primaryDark</item>
        <item name="android:colorAccent">@color/tertiary_colour</item>
        <item name="android:colorButtonNormal">@color/tertiary_colour</item>
        <item name="android:navigationBarColor">@color/tertiary_colour</item>
    </style>
</resources>
